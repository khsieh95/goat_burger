<h1>Burgers!</h1>

<div class="container" id = "container1">
<h2>Burgers that haven't been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burgers-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>



<h2>Burgers devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/burgers-block devour=false}}
    {{/if}}
  {{/each}}
</ul>


<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devour?</label><br>
    <input type="radio" name="devour" value="1" checked> Devour!<br>
    <input type="radio" name="devour" value="0"> Don't devour!
  </div>

  <button type="submit">Add Burger</button>
</form>

</div>